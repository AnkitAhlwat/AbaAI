import { Box } from "@mui/material";

const SideBar = () => {
  return (
    <Box
      sx={{
        backgroundColor: "#262522",
        borderRadius: "5px",
        height: "100%",
      }}
    ></Box>
  );
};

export default SideBar;
